\section{Импульс}

\subsection{Определение импульса}
\textbf{Импульс материальной точки} --- векторная физическая величина, являющаяся мерой механического движения и равная произведению массы точки на её скорость:
\[
    \vec{p} = m\vec{v}
\]

Согласно второму закону Ньютона в дифференциальной форме, скорость изменения импульса тела равна действующей на него силе:
\[
    \frac{d\vec{p}}{dt} = \vec{F}
\]
Эта запись является более общей, чем $\vec{F} = m\vec{a}$, так как применима и в случаях переменной массы.

\subsection{Центр масс системы}
\textbf{Центром масс} системы материальных точек называется точка $C$, радиус-вектор которой определяется как:
\[
    \vec{r}_{cm} = \frac{\sum m_i \vec{r}_i}{\sum m_i} = \frac{1}{M} \sum m_i \vec{r}_i
\]
Движение абсолютно твердого тела можно полностью описать как движение его центра масс и вращение вокруг него:

\begin{center}
\begin{tblr}{
  colspec = {X[1.2,c]X[2,c]},
  vlines,
  hlines,
  row{1} = {font=\bfseries,mode=text},
  row{odd} = {bg=gray!10},
  cells = {mode=math,c,m},
  rowsep = 6pt
}
\text{Центр масс системы} & \text{Формулы и свойства} \\
\text{Скорость системы} & \vec{V}_c = \dfrac{\diff\vec{R}}{\diff t} = \dfrac{\sum m_i\vec{v}_i}{\sum m_i} \\
\text{Импульс системы} & \vec{P}_c = M\vec{V}_c = \sum m_i\vec{v}_i
\end{tblr}
\end{center}

\subsection{Закон сохранения импульса}
В \textbf{изолированной системе} не действуют внешн. силы или их сумма нулевая:
\[\sum \vec{F} = 0\]

Для системы материальных точек:
\[
    \frac{d\vec{P}}{dt} = \sum \vec{F}
\]
Если система изолированная, то $\frac{d\vec{P}}{dt} = 0$, следовательно:
\[\boxed{
    \vec{P} = \sum m_i \vec{v}_i = \text{const}
}\]
То есть центр масс замкнутой системы движется равномерно и прямолинейно или покоится.

\subsection{Уравнение Мещерского}

\subsubsection{Формулировка}
Уравнение описывает движение тела с переменной массой (например, ракеты):

\begin{center}
\begin{tblr}{
  colspec = {X[1.2,l]X[1,c]},
  vlines,
  hlines,
  row{1} = {font=\bfseries,mode=text},
  row{odd} = {bg=gray!10},
  cells = {c,m},
  rowsep = 8pt,
  width = \textwidth
}
Уравнение Мещерского & Схема процессов массы \\
\parbox{0.5\textwidth}{
\centering
\large
$m(t)\dfrac{\diff\vec{v}}{\diff t}=\vec{u_1}\dfrac{\diff m_1}{\diff t}-\vec{u_2}\dfrac{\diff m_2}{\diff t}+\vec{F}$ \\
\normalsize
\begin{align*}
m(t) &- \text{мгновенная масса} \\
\vec{u_1} &= \vec{v}_1 - \vec{v} \quad \text{(отн. присоединение)} \\
\vec{u_2} &= \vec{v}_2 - \vec{v} \quad \text{(отн. выброс)} \\
\vec{F} &- \text{внешние силы}
\end{align*}
}
&
\begin{minipage}[c]{\linewidth}
\centering
\begin{tikzpicture}[scale=1.0, >=stealth]
    % Основное тело с штриховкой для ч/б печати
    \draw[fill=black!10, thick] (0,0) circle (0.7);
    \node at (0,0) {$m(t)$};
    
    % Стрелка движения тела (сплошная толстая)
    \draw[->, ultra thick] (0.9,0) -- (2.0,0) node[above] {$\vec{v}$};
    
    % Присоединяемая масса (штрих-пунктир)
    \draw[->, thick, densely dashed] (-2.0,1.0) -- (-0.8,0.3);
    \node[left] at (-2.0,1.0) {$\vec{u_1}$};
    \node[above] at (-1.4,1.2) {$\frac{\diff m_1}{\diff t}$};
    
    % Отделяемая масса (пунктир)
    \draw[->, thick, dashed] (-0.8,-0.3) -- (-2.0,-1.0);
    \node[left] at (-2.0,-1.0) {$\vec{u_2}$};
    \node[below] at (-1.4,-1.2) {$\frac{\diff m_2}{\diff t}$};
    
    % Внешние силы (двойная линия)
    \draw[->, ultra thick] (0,0.9) -- (0,1.8);
    \node[right] at (0,1.3) {$\vec{F}$};
\end{tikzpicture}
\end{minipage}
\\
\end{tblr}
\end{center}

\subsubsection{Вывод}

Рассмотрим тело переменной массы $m(t)$, движущееся со скоростью $\vec{v}(t)$. Пусть за время $dt$ к телу присоединяется масса $dm_1 > 0$ со скоростью $\vec{v}_1$ относительно Земли, а отделяется масса $dm_2 > 0$ со скоростью $\vec{v}_2$ относительно Земли.

Введем относительные скорости:
\begin{align*}
\vec{u}_1 &= \vec{v}_1 - \vec{v} \text{ --- относительная скорость присоединяемой массы}, \\
\vec{u}_2 &= \vec{v}_2 - \vec{v} \text{ --- относительная скорость отделяемой массы}.
\end{align*}

Тогда:
\[
\vec{v}_1 = \vec{v} + \vec{u}_1, \qquad \vec{v}_2 = \vec{v} + \vec{u}_2.
\]

Изменение массы тела за время $dt$:
\[
dm = dm_1 - dm_2
\]

Импульс системы в момент времени $t$:
\[
\vec{p}(t) = m\vec{v}.
\]

Импульс системы в момент времени $t+dt$:
\begin{align*}
\vec{p}(t+dt) &= (m + dm)(\vec{v} + d\vec{v}) + (-dm_1)\vec{v}_1 + (+dm_2)\vec{v}_2 \\
&= (m + dm)(\vec{v} + d\vec{v}) - dm_1(\vec{v} + \vec{u}_1) + dm_2(\vec{v} + \vec{u}_2).
\end{align*}

Изменение импульса под действием внешней силы $\vec{F}$:
\[
\vec{F}\, dt = \vec{p}(t+dt) - \vec{p}(t).
\]

Подставляем выражения для импульсов:
\begin{align*}
\vec{F}\, dt &= \big[(m + dm)(\vec{v} + d\vec{v}) - dm_1(\vec{v} + \vec{u}_1) + dm_2(\vec{v} + \vec{u}_2)\big] - m\vec{v} \\
&= m\vec{v} + m d\vec{v} + \vec{v}\, dm + dm\, d\vec{v} - \vec{v}\, dm_1 - \vec{u}_1 dm_1 + \vec{v}\, dm_2 + \vec{u}_2 dm_2 - m\vec{v} \\
&= m d\vec{v} + \vec{v}(dm - dm_1 + dm_2) + \vec{u}_2 dm_2 - \vec{u}_1 dm_1 + dm\, d\vec{v}.
\end{align*}

Учитывая, что $dm = dm_1 - dm_2$, получаем $dm - dm_1 + dm_2 = 0$. Пренебрегая членом второго порядка $dm\, d\vec{v}$, имеем:
\[
\vec{F}\, dt = m d\vec{v} + \vec{u}_2 dm_2 - \vec{u}_1 dm_1.
\]

Разделим на $dt$:
\[
\vec{F} = m \frac{d\vec{v}}{dt} + \vec{u}_2 \frac{dm_2}{dt} - \vec{u}_1 \frac{dm_1}{dt}.
\]

Перегруппировав слагаемые, получаем:
\[\boxed{
m(t) \frac{d\vec{v}}{dt} = \vec{u}_1 \frac{dm_1}{dt} - \vec{u}_2 \frac{dm_2}{dt} + \vec{F}
}\]

\subsubsection{Катер с пассивным ротором}

Катер движется в воде. Двигатель (пассивный ротор) захватывает набегающую воду через отверстие площадью $S$ и выбрасывает её назад с относительной скоростью $u$. Сопротивление среды является квадратичным: $F_{\text{сопр}} = kv^2$. Найти максимально возможную скорость катера $V_{max}$:

\begin{center}
  \begin{tikzpicture}[>=Stealth]

    % --- Стили для ч/б печати ---
    \tikzstyle{force} = [ultra thick, ->, line width=1.5pt]
    \tikzstyle{velocity} = [thick, ->, line width=1pt]
    \tikzstyle{flow path} = [thick, ->, dash pattern=on 4pt off 2pt, line width=0.8pt]
    \tikzstyle{hull style} = [line width=1.2pt, fill=black!10]
    \tikzstyle{rotor style} = [line width=1pt, fill=white]

    % --- Фон (вода с узором) ---
    \fill[pattern=north east lines, pattern color=black!20] (-3.5,-1.8) rectangle (3.5, 0);
    \draw[black!40, decorate, decoration={waves, segment length=4mm, amplitude=0.5mm}] (-3.5, 0) -- (3.5, 0);

    % --- Катер (корпус с узором) ---
    \fill[pattern=horizontal lines, pattern color=black!20] (-2, 0) -- (-1.8, 0.7) -- (1.8, 0.7) -- (2, 0) -- cycle;
    \draw[hull style] (-2, -0.1) -- (-1.8, 0.7) -- (1.8, 0.7) -- (2, -0.1) -- cycle;
    \node[black] at (0, 0.35) {\textbf{Катер}};

    % --- Роторный механизм (схема снизу) ---
    % Канал ротора с узором
    \fill[pattern=vertical lines, pattern color=black!15] (-1.5, -0.1) -- (1.5, -0.1) -- (1.5, -0.8) -- (-1.5, -0.8) -- cycle;
    \draw[rotor style] (-1.5, -0.1) -- (1.5, -0.1) -- (1.5, -0.8) -- (-1.5, -0.8) -- cycle;
    
    % Обозначение площади заборного отверстия S (тонкие линии)
    \draw[<->, line width=0.6pt] (2.9, -0.1) -- (2.9, -0.8) node[midway, right] {$S$};

    % Линия потока внутри (штриховая)
    \draw[flow path] (1.5, -0.45) -- (-1.5, -0.45);


    % --- Скорости (в системе отсчета катера) ---
    
    % Входящий поток воды (скорость v) - сплошная стрелка
    \draw[velocity] (3.0, -0.45) -- (1.7, -0.45) node[midway, above] {$\vec{u}_{\text{вх}}$};
    \node[align=center, font=\footnotesize] at (2.4, -1.3) {Набегающий\\поток};

    % Выходящий поток воды (относительная скорость u) - штриховая стрелка
    \draw[velocity, dash pattern=on 3pt off 2pt] (-1.7, -0.45) -- (-3.0, -0.45) node[midway, above] {$\vec{u}_{\text{вых}}$};
    \node[align=center, font=\footnotesize] at (-2.4, -1.3) {Реактивная\\струя};

    % Скорость катера (очень толстая стрелка)
    \draw[line width=1.8pt, ->] (0, 1.0) -- (2, 1.0) node[midway, above] {$\vec{V}_{\text{max}}$};


    % --- Силы ---
    
    % Сила сопротивления (штриховая толстая стрелка)
    \draw[force, dash pattern=on 5pt off 3pt] (-0.5, 0.35) -- (-2.5, 0.35) node[midway, above] {$\vec{F}_{\text{сопр}}$};
    
    % Реактивная сила тяги (сплошная толстая стрелка)
    \draw[force] (0, -0.45) -- (1.7, -0.45) node[midway, below, yshift=-2mm] {$\vec{F}_{\text{реак}}$};

\end{tikzpicture}
\end{center}

Расход массы воды $\mu = \frac{dm}{dt}$ определяется скоростью набегающего потока, которая в системе отсчета катера равна его мгновенной скорости $v$:
\[
    \mu(v) = \rho S v
\]
Это означает, что при $v = 0$ тяга двигателя отсутствует.

Согласно уравнению Мещерского для установившегося движения ($a = 0$), реактивная сила тяги должна быть уравновешена силой сопротивления:
\[ F_{\text{реак}} = F_{\text{сопр}} \]

Реактивная сила определяется как произведение секундного расхода массы на изменение относительной скорости воды (относительно катера вода заходит со скоростью $v$ и выходит со скоростью $u$):
\[ F_{\text{реак}} = \mu \cdot (u_{\text{вых}} - u_{\text{вх}}) = (\rho S v) \cdot (u - v) \]

Запишем полное уравнение равновесия:
\[
    \rho S v (u - v) = k v^2\implies \rho S u v - \rho S v^2 = k v^2
\]

Поскольку нас интересует установившееся движение с ненулевой скоростью, мы можем разделить обе части уравнения на $v$:
\[ \rho S (u - v) = k v \]

Сгруппируем слагаемые с искомой скоростью $V_{max}$:
\[ \rho S u - \rho S V_{max} = k V_{max}\implies \rho S u = V_{max} (k + \rho S) \]

Выразим искомую максимальную скорость:
\[
    \boxed{V_{max} = \frac{\rho S u}{\rho S + k}}
\]