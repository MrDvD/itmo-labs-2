#ifndef ITERATIONS_H
#define ITERATIONS_H

#include "preamble.h"
#include <unordered_map>

vector<double> solve_by_fixed_point_iterations(int system_id, int number_of_unknowns, vector<double> x);

#endif