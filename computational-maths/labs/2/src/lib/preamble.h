#ifndef PREAMBLE_H
#define PREAMBLE_H

#include <bits/stdc++.h>

#include <iostream>

using namespace std;

string ltrim(const string&);
string rtrim(const string&);
vector<string> split(const string&);

inline bool isSolutionExists = true;
inline string errorMessage = "";

#endif